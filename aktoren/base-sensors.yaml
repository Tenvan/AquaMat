# Definition der Sensoren

# Base Sensors for all AquaMats
text_sensor:
  - platform: template
    id: pump1_status
    name: Pump 1 Status
    lambda: |-
      return {"aus"};

  - platform: template
    id: pump2_status
    name: Pump 2 Status
    lambda: |-
      return {"aus"};

  - platform: template
    id: pump3_status
    name: Pump 3 Status
    lambda: |-
      return {"aus"};

